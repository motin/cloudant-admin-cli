{
  "name": "cloudant-admin-cli",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "format": "prettier './**/*.{css,ts,js,json,md}' --trailing-comma=all --ignore-path=.prettierignore --write",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "dependencies": {
    "@cloudant/cloudant": "^2.3.0",
    "argparse": "^1.0.10",
    "dotenv-safe": "^6.0.0"
  },
  "devDependencies": {
    "prettier": "^1.14.3"
  }
}
